.entry {
  &__item {
    position: relative;
  }
  
  &__item:not(:first-child) {
    margin-top: 30px;
  }
  
  &__error {
    position: absolute;
    top: -23px;
    left: 15px;
    font-size: 14px;
    color: var(--main-red-color);
  }
}
