.post-owner {
  display: flex;
  align-items: center;
  gap: 10px;

  &__avatar {
    min-width: 40px;
    max-width: 40px;
    min-height: 40px;
    max-height: 40px;
    border-radius: 50%;
    background-color: var(--layout-black);
  }

  &__name {
    font-size: 16px;
  }
}
