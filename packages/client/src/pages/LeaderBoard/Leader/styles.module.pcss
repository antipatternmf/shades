.leader {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
  padding: 6px 12px;

  &__name {
    font-size: 14px;
  }

  &__score {
    color: var(--fawn);
  }
}
